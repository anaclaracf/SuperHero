
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Turret : MonoBehaviour
{
    //public Rigidbody Bullet;
    //public float MoveSpeed = 3.0f;

    //Vector3 direcao;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // // Player perto do livro
    // private void OnCollisionEnter(Collider other)
    // {
    //     if(other.gameObject.tag == "Teste"){

    //         print("Entrou");
    //     }
    //     // Rigidbody instantiatedProjectile = Instantiate(Bullet, transform.position, transform.rotation) as Rigidbody;
    //     // instantiatedProjectile.velocity = transform.TransformDirection(new Vector3(0, 0,MoveSpeed));
    // }

    // // Player longe do livro
    // private void OnTriggerExit(Collider other)
    // {
       
    // }
}